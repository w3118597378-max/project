<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cl.dao.ForumReportDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.cl.entity.ForumReportEntity" id="forumReportMap">
        <result property="forumId" column="forum_id"/>
        <result property="title" column="title"/>
        <result property="reportUserid" column="report_userid"/>
        <result property="reportUsername" column="report_username"/>
        <result property="reportedUserid" column="reported_userid"/>
        <result property="reportedUsername" column="reported_username"/>
        <result property="reason" column="reason"/>
        <result property="images" column="images"/>
        <result property="suggestion" column="suggestion"/>
        <result property="status" column="status"/>
        <result property="reportType" column="report_type"/>
        <result property="userid" column="userid"/>
    </resultMap>

    <select id="selectListView"
		resultType="com.cl.entity.view.ForumReportView" >

		SELECT  forum_report.* FROM forum_report  forum_report 	        
        <where> 1=1 ${ew.sqlSegment}</where>
	</select>
	
	<select id="selectView"
		resultType="com.cl.entity.view.ForumReportView" >
		SELECT * FROM forum_report  forum_report <where> 1=1 ${ew.sqlSegment}</where>
	</select>



</mapper>
